spring:
  datasource:
    url: jdbc:mysql://localhost:3306/daily?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimeZone=Asia/Seoul
    username: root
    password: rladmlqls335
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace